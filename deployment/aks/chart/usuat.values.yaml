julu-deploy:
  env:
    # -- Environment for the application
    prouat
  region:
    - europe: { dns: juu.eu }
    - us:
        dns: julu.com.eu
    - asia:
        dns: julu.com.ap
  # -- Application name
  appName: commitapp
  podResources:
    requests:
      cpu: 8
